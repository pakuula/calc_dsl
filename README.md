# Калькулятор DSL — Язык для математических вычислений

<div align="center">

**Мощный скриптовый язык для математических вычислений с поддержкой циклов, функций и произвольной точности**

[![Python 3.13+](https://img.shields.io/badge/python-3.13+-blue.svg)](https://www.python.org/downloads/)
[![Tests](https://img.shields.io/badge/tests-14%20passed-brightgreen.svg)](tests/)
[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)

</div>

## 📋 Содержание

- [О проекте](#о-проекте)
- [Возможности](#возможности)
- [Установка](#установка)
- [Быстрый старт](#быстрый-старт)
- [Синтаксис языка](#синтаксис-языка)
- [Использование CLI](#использование-cli)
- [Примеры](#примеры)
- [Отладка с трассировкой](#отладка-с-трассировкой)
- [Тестирование](#тестирование)
- [Архитектура](#архитектура)

## О проекте

Калькулятор DSL — это предметно-ориентированный язык (Domain-Specific Language) для математических вычислений, разработанный для выполнения сложных арифметических операций с произвольной точностью. Язык поддерживает переменные, циклы, функции и модульную арифметику.

Проект был создан с использованием четырёхагентной методологии разработки:
- **@expert** — разработка спецификации языка
- **@designer** — проектирование грамматики с использованием Lark
- **@coder** — реализация интерпретатора на Python
- **@tester** — написание тестов с использованием pytest

## Возможности

### ✨ Основные возможности

- **Арифметика произвольной точности** — вычисления с `Decimal` типом
- **Математические функции** — `sin`, `cos`, `tg`, `ctg`, `ln`, `log2`, `log10`, `sqrt`, `nrt`
- **Модульная арифметика** — оператор `mod` с корректной семантикой для отрицательных чисел
- **Оптимизация степени по модулю** — `a**b mod p` использует встроенный `pow(a, b, p)`
- **Циклы for** — с поддержкой шага и использования в качестве выражений
- **Блоки как выражения** — результат блока равен его последнему выражению
- **Управление точностью** — функции `set_precision(n)` и `get_precision()`
- **Комментарии** — строки после `#` игнорируются
- **Трассировка выполнения** — режим отладки `--trace` для пошагового анализа

### 🎯 Операторы

- Арифметические: `+`, `-`, `*`, `/`, `**` (степень), `mod`
- Присваивания: `=`, `+=`, `-=`, `/=`, `mod=`
- Приоритет: стандартная математическая иерархия
- Ассоциативность: `mod` левоассоциативен, `**` правоассоциативен

### 🔢 Числа и константы

- Числа: целые и дробные с произвольной точностью (по умолчанию 10 знаков)
- Константы: `pi` (π), `e` (число Эйлера)

## Установка

### Требования

- Python 3.13 или выше
- pip (менеджер пакетов Python)

### Шаги установки

```bash
# Клонируйте репозиторий
cd /path/to/project

# Создайте виртуальное окружение
python -m venv .venv

# Активируйте виртуальное окружение
source .venv/bin/activate  # Linux/macOS
# или
.venv\Scripts\activate     # Windows

# Установите зависимости
pip install lark pytest
```

## Быстрый старт

### Создайте файл `hello.clc`

```python
# Простое вычисление
print("Привет, мир калькуляторов!")
x = 2 + 2 * 2
print("2 + 2 * 2 =", x)

# Модульная арифметика
result = 2 ** 10 mod 7
print("2^10 mod 7 =", result)
```

### Запустите скрипт

```bash
python cli.py hello.clc
```

**Вывод:**
```
Привет, мир калькуляторов!
2 + 2 * 2 = 6.0000000000
2^10 mod 7 = 2.0000000000
```

## Синтаксис языка

### Переменные и присваивания

```python
# Простое присваивание
x = 10
y = 20

# Сокращённые формы
x += 5      # x = x + 5
y -= 3      # y = y - 3
x /= 2      # x = x / 2
y mod= 7    # y = y mod 7
```

### Математические операции

```python
# Базовые операции
a = 2 + 3 * 4           # 14
b = 2 ** 10             # 1024
c = 17 mod 5            # 2

# Отрицательные числа и mod
d = -5 mod 3            # 1 (не -2!)
e = 5 mod -3            # 2

# Оптимизированная модульная степень
power_mod = 2 ** 100 mod 13    # Быстрое вычисление
```

### Математические функции

```python
# Тригонометрия
angle = pi / 4
sin_val = sin(angle)
cos_val = cos(angle)
tan_val = tg(angle)
cot_val = ctg(angle)

# Логарифмы
ln_e = ln(e)           # 1
log2_8 = log2(8)       # 3
log10_100 = log10(100) # 2

# Корни
sqrt_16 = sqrt(16)     # 4
nrt_27_3 = nrt(27, 3)  # 3 (кубический корень)
```

### Управление точностью

```python
# Получить текущую точность (по умолчанию 10)
p = get_precision()
print("Текущая точность:", p)

# Установить точность в 5 знаков
set_precision(5)
result = 1 / 3         # 0.33333

# Вернуть точность обратно
set_precision(10)
```

### Блоки

```python
# Блок как выражение
result = (
    a = 2
    b = 3
    a * b    # Последнее выражение — результат блока
)
print(result)  # 6
```

### Циклы for

```python
# Простой цикл
sum = 0
for i in 1 .. 10 (
    sum += i
)
print("Сумма 1..10 =", sum)  # 55

# Цикл с шагом
for i in 0 .. 10 by 2 (
    print(i)  # 0, 2, 4, 6, 8, 10
)

# Обратный цикл
for i in 10 .. 1 by -1 (
    print(i)  # 10, 9, 8, ..., 1
)

# Цикл как выражение
factorial = (
    result = 1
    for i in 1 .. 5 (
        result *= i
        result
    )
)
print("5! =", factorial)  # 120
```

### Вывод с print

```python
# Вывод нескольких значений
x = 42
print("Ответ:", x)

# Вывод строки и чисел
print("Результат вычисления 2 + 2 =", 2 + 2)

# Только строка
print("Сообщение")
```

### Комментарии

```python
# Это однострочный комментарий
x = 5  # Комментарий после кода

# Комментарии игнорируются при разборе
# y = 10  # Эта строка не выполнится
```

### Разделители

```python
# Новая строка разделяет инструкции
x = 1
y = 2

# Точка с запятой также может использоваться
a = 3; b = 4; c = a + b
```

## Использование CLI

### Основная команда

```bash
python cli.py <script.clc> [переменные...] [--trace]
```

### Параметры

- `script.clc` — путь к файлу скрипта (обязательно)
- `переменные` — переопределение переменных в формате `имя=значение`
- `--trace` — включить режим трассировки для отладки

### Примеры использования

#### Базовый запуск

```bash
python cli.py fib.clc
```

#### Передача переменных

```bash
# Вычислить Фибоначчи для n=20
python cli.py fib.clc n=20

# Передать несколько переменных
python cli.py script.clc x=10 y=5 z=3
```

#### Режим трассировки

```bash
python cli.py fib.clc n=5 --trace
```

### Справка

```bash
python cli.py --help
```

**Вывод:**
```
usage: cli.py [-h] [--trace] script [vars ...]

Run DSL scripts.

positional arguments:
  script      Path to .clc script
  vars        Variable overrides: name=value

options:
  -h, --help  show this help message and exit
  --trace     Enable trace mode for debugging
```

## Примеры

### 1. Числа Фибоначчи ([fib.clc](fib.clc))

```python
print("n=", n)

prev=0
curr=1

fib = for i in (2-1) .. n-1 (
    next = curr + prev
    print("i=", i, "next=", next)
    prev = curr
    curr = next
    curr
)

fib
```

**Запуск:**
```bash
python cli.py fib.clc n=10
```

### 2. Модульная арифметика

```python
# Проверка малой теоремы Ферма: a^(p-1) ≡ 1 (mod p)
a = 2
p = 13  # простое число

result = a ** (p - 1) mod p
print("2^12 mod 13 =", result)  # Должно быть 1
```

### 3. Вычисление факториала

```python
n = 10
fact = 1
for i in 1 .. n (
    fact *= i
)
print(n, "! =", fact)
```

### 4. Сумма геометрической прогрессии

```python
# Сумма 2^0 + 2^1 + ... + 2^n
n = 10
sum = 0
for i in 0 .. n (
    sum += 2 ** i
)
print("Сумма =", sum)  # 2^(n+1) - 1 = 2047
```

### 5. Тригонометрические вычисления

```python
# Таблица синусов
print("Угол (рад)", "sin", "cos")
for i in 0 .. 6 (
    angle = pi * i / 6
    print(angle, sin(angle), cos(angle))
)
```

## Отладка с трассировкой

Режим трассировки (`--trace`) показывает пошаговое выполнение программы с подробной информацией о каждой операции.

### Формат вывода трассировки

```
- номер_строки: текст_инструкции     # Перед выполнением
+ переменная = значение               # После присваивания
+ значение                            # После вычисления выражения
```

### Пример трассировки

**Файл `example.clc`:**
```python
x = 5
y = 10
sum = 0
for i in 1 .. 3 (
    sum += i
)
sum
```

**Команда:**
```bash
python cli.py example.clc --trace
```

**Вывод:**
```
- 1: x = 5
+ x = 5.0000000000
- 2: y = 10
+ y = 10.0000000000
- 3: sum = 0
+ sum = 0.0000000000
- 4: for i in 1 .. 3 (
- 4: for i in 1.0000000000 .. 3.0000000000
+ i = 1.0000000000
- 5: sum += i
+ sum = 1.0000000000
+ i = 2.0000000000
- 5: sum += i
+ sum = 3.0000000000
+ i = 3.0000000000
- 5: sum += i
+ sum = 6.0000000000
- 7: sum
+ 6.0000000000
6.0000000000
```

### Когда использовать трассировку

- Отладка логических ошибок в циклах
- Проверка значений переменных на каждом шаге
- Понимание порядка выполнения операций
- Отладка сложных вложенных выражений
- Обучение работе с языком

## Тестирование

Проект включает полный набор тестов, покрывающих все возможности языка.

### Запуск тестов

```bash
# Запустить все тесты
python -m pytest tests/ -v

# Запустить конкретный тест
python -m pytest tests/test_interpreter.py::test_negative_mod_semantics -v

# Запустить с покрытием кода
python -m pytest tests/ --cov=interpreter --cov-report=html
```

### Покрытие тестами

Текущие тесты (14 шт.) покрывают:

- ✅ Приоритет операторов
- ✅ Приоритет степени и mod
- ✅ Цепочки присваиваний
- ✅ Математические функции (ln, log2, sin, cos, tg, ctg, sqrt, nrt)
- ✅ Деление на ноль
- ✅ Ошибки домена функций
- ✅ Неопределённые переменные
- ✅ Семантика отрицательного mod
- ✅ Результаты for-выражений
- ✅ For-выражения с вычисляемыми диапазонами
- ✅ Видимость переменной цикла (предопределённая)
- ✅ Видимость переменной цикла (новая)
- ✅ Сохранение значения при невыполненном цикле
- ✅ Строки в print

### Пример теста

```python
def test_negative_mod_semantics():
    interpreter = Interpreter()
    # -5 mod 3 должно быть 1, не -2
    result = interpreter.execute("-5 mod 3")
    assert result == Decimal("1")
    
    # 5 mod -3 должно быть 2 (модуль по |3|)
    result = interpreter.execute("5 mod -3")
    assert result == Decimal("2")
```

## Архитектура

### Компоненты проекта

```
.
├── README.md               # Документация (этот файл)
├── SPECIFICATION.md        # Спецификация языка
├── grammar.lark            # Грамматика Lark (Earley parser)
├── interpreter.py          # Интерпретатор
├── cli.py                  # Интерфейс командной строки
├── samples.md              # Примеры кода
├── fib.clc                 # Пример: числа Фибоначчи
├── tests/
│   └── test_interpreter.py # Тесты pytest
└── .venv/                  # Виртуальное окружение Python
```

### Технологический стек

- **Python 3.13+** — язык реализации
- **Lark** — парсер (алгоритм Earley для обработки неоднозначности)
- **Decimal** — арифметика произвольной точности
- **pytest** — фреймворк тестирования

### Парсер

Грамматика использует **Earley parser** из библиотеки Lark для корректной обработки:
- Унифицированного токена `SEP` (`;` или `\n+`)
- Выражений в диапазонах for-циклов
- Блоков как выражений с произвольными разделителями

### Интерпретатор

Основные классы:

- `Interpreter` — главный класс интерпретатора
- `DSLError` — базовый класс исключений
- `DivisionByZeroError` — деление на ноль
- `VariableNotFoundError` — неопределённая переменная
- `PowerValue` — оптимизация для `a**b mod p`

### Особенности реализации

1. **Семантика mod** — использует `ROUND_FLOOR` для гарантии неотрицательного остатка
2. **Оптимизация степени** — `a**b mod p` использует встроенный `pow(a, b, p)` для больших чисел
3. **Видимость переменных цикла** — сложная логика сохранения/удаления после цикла
4. **Точность** — каждая операция округляется до текущей точности с `ROUND_HALF_UP`

## Семантика mod для отрицательных чисел

В отличие от многих языков, этот DSL использует математическую семантику `mod`:

```python
# Python: -5 % 3 == -2
# Наш DSL: -5 mod 3 == 1

# Формула: result = left - floor(left / |modulus|) * |modulus|
```

Результат всегда в диапазоне `[0, |modulus|)`.

## Видимость переменной цикла

Переменная цикла имеет специальную семантику:

```python
# Случай 1: переменная не определена до цикла
for i in 1..3 (print(i))
# print(i)  # Ошибка: i не определена после цикла

# Случай 2: переменная определена до цикла
i = 0
for i in 1..3 (print(i))
print(i)  # OK: i = 3 (последнее значение)

# Случай 3: цикл не выполнился
i = 100
for i in 1..0 (print(i))  # Не выполнится
print(i)  # OK: i = 100 (исходное значение сохранено)
```

## Обработка ошибок

Все ошибки включают информацию о позиции в исходном коде:

```python
x = 10 / 0
# Ошибка: division by zero (line 1, column 5)

y = sqrt(-1)
# Ошибка: sqrt domain error (line 1, column 5)

z = undefined_var
# Ошибка: Variable not found: undefined_var (line 1, column 5)
```

## Производительность

- Использование `Decimal` вместо `float` гарантирует точность, но медленнее
- Оптимизация `pow(a, b, p)` для больших модульных степеней
- Парсер кэширует результаты для ускорения повторных вычислений

## Лицензия

MIT License — свободное использование, модификация и распространение.

## Авторы

Разработано с использованием методологии мультиагентной разработки:
- Спецификация: @expert
- Грамматика: @designer  
- Реализация: @coder
- Тестирование: @tester
- Документация: @writer

## Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте [примеры](#примеры)
2. Запустите с флагом `--trace` для отладки
3. Изучите [тесты](tests/test_interpreter.py) для понимания возможностей

---

**Приятных вычислений! 🧮**
