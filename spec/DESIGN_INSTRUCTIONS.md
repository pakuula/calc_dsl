# 🎨 Дизайн: Условные выражения и булевские операторы

**Статус:** ✅ Дизайн завершен (готово к реализации)

**Для кого:** @coder (реализация)

**От кого:** @designer (проектирование)

---

## 📚 Полная документация дизайна

### Начните отсюда:

1. **[DESIGN_SUMMARY.md](DESIGN_SUMMARY.md)** ← **ЧИТАЙТЕ СНАЧАЛА**
   - Обзор всего проекта
   - Ключевые решения
   - Контрольный список
   - Ссылки на все другие документы

2. **[DESIGN_CONDITIONALS.md](DESIGN_CONDITIONALS.md)** — Требования
   - Полное описание функциональности
   - Приоритеты операторов (таблица)
   - Архитектура с диаграммами
   - Примеры синтаксиса

3. **[IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md)** — Код и архитектура
   - Обновления `grammar.lark`
   - 5 новых методов с полным кодом
   - Обновления существующих методов
   - Таблица трансформации синтаксиса

4. **[OPERATOR_PRECEDENCE.md](OPERATOR_PRECEDENCE.md)** — Приоритеты
   - Визуальная диаграмма (9 уровней)
   - Примеры парсинга
   - Правила вывода типов
   - Таблица ошибок

5. **[SYNTAX_EXAMPLES.md](SYNTAX_EXAMPLES.md)** — Примеры
   - 6 категорий примеров кода
   - Практические примеры
   - Таблица тестовых выражений
   - Ошибки и граничные случаи

6. **[TEST_SCENARIOS.md](TEST_SCENARIOS.md)** — Тесты
   - 57 готовых тестов
   - Код для pytest
   - 6 категорий (базовые, сравнения, логика, ошибки, комплекс, граница)

---

## 🎯 Краткое описание

Добавите в DSL три новые функции:

### 1. Условные выражения

```clc
result = 10 if condition else 20
nested = 5 if x >= 90 else (4 if x >= 80 else 3)
```

### 2. Операторы сравнения

```clc
a = 100 if x < y else 0        # <, <=, >, >=, ==, !=
b = 100 if x == 5 else 0
c = 100 if x != y else 0
```

### 3. Логические операторы

```clc
d = 100 if x < y and y < z else 0     # and
e = 100 if x < 5 or y > 20 else 0     # or
f = 100 if not (x == 0) else 0        # not
```

**Важное ограничение**: Булевские значения допустимы **ТОЛЬКО** в условных выражениях!

```clc
x = (1 < 2)              # ❌ ОШИБКА!
y = (1 < 2) + 5          # ❌ ОШИБКА!
z = 10 if (1 < 2) else 5 # ✅ ОК
```

---

## 🏗️ Архитектура

### Файлы для изменения

```
/home/nikolay/zzz/c++/essential_1/
├── grammar.lark           ← Обновить: +20 строк (новые правила выражений)
├── interpreter.py         ← Обновить: +200 строк (5 новых методов)
├── tests/test_interpreter.py  ← Обновить: +300 строк (57 новых тестов)
└── README.md              ← Обновить: +100 строк (примеры)
```

### Что добавить

| Компонент | Типа | Строк | Описание |
|-----------|------|-------|---------|
| grammar.lark | Правила | +20 | Новые уровни выражений |
| BooleanContextError | Класс | +8 | Новое исключение |
| _eval_conditional_expr() | Метод | +25 | Условные выражения |
| _eval_or_expr() | Метод | +20 | Логический OR |
| _eval_and_expr() | Метод | +20 | Логический AND |
| _eval_not_expr() | Метод | +15 | Логический NOT |
| _eval_comparison() | Метод | +30 | Сравнения |
| Обновления _eval() | Код | +10 | Обработка новых типов |
| Проверки типов | Код | +30 | В арифметических методах |
| Тесты | pytest | +300 | 57 новых тестов |

**Итого:** ~120 строк новых правил и методов

---

## 📋 Порядок реализации

### Шаг 1: Грамматика (1 час)

**Файл:** `grammar.lark`

**Что делать:**
1. Заменить правило `?expr:` на новую иерархию
2. Добавить правила для `conditional_expr`, `or_expr`, `and_expr`, `not_expr`, `comparison`
3. Добавить токен `COMP_OP`
4. Протестировать парсинг примеров из SYNTAX_EXAMPLES.md

**Результат:** Парсер может разобрать условные выражения

### Шаг 2: Новые методы интерпретатора (2 часа)

**Файл:** `interpreter.py`

**Что делать:**
1. Добавить класс `BooleanContextError`
2. Добавить 5 методов:
   - `_eval_conditional_expr()`
   - `_eval_or_expr()`
   - `_eval_and_expr()`
   - `_eval_not_expr()`
   - `_eval_comparison()`
3. Полные реализации есть в `IMPLEMENTATION_PLAN.md`

**Результат:** Интерпретатор может вычислять условные выражения

### Шаг 3: Обновить _eval() (30 минут)

**Файл:** `interpreter.py`

**Что делать:**
1. В методе `_eval()` добавить обработку новых типов узлов
2. Добавить elif-блоки для каждого нового типа:
   - `"conditional_expr"` → `_eval_conditional_expr()`
   - `"or_expr"` → `_eval_or_expr()`
   - и т.д.

**Результат:** Новые типы узлов корректно обрабатываются

### Шаг 4: Проверка типов (1 час)

**Файл:** `interpreter.py`

**Что делать:**
1. Обновить `_eval_assignment()` - отклонять boolean значения
2. Обновить `_eval_sum()`, `_eval_product()`, `_eval_power()` - проверять операнды
3. Проверить, что boolean не может быть в арифметике

**Результат:** Система типов работает, ошибки обнаруживаются

### Шаг 5: Тестирование (2 часа)

**Файл:** `tests/test_interpreter.py`

**Что делать:**
1. Добавить 57 тестов из `TEST_SCENARIOS.md`
2. Запустить все тесты: `pytest tests/test_interpreter.py -v`
3. Проверить, что старые тесты продолжают работать
4. Проверить mypy: `mypy interpreter.py`

**Результат:** Все 57 новых + 19 старых = 76 тестов проходят

### Шаг 6: Документация (30 минут)

**Файлы:** `README.md`

**Что делать:**
1. Добавить раздел про условные выражения
2. Добавить примеры из `SYNTAX_EXAMPLES.md`
3. Обновить таблицу таких возможностей
4. Добавить примеры программ (conditional.clc, logic.clc)

**Результат:** Пользователи понимают, как использовать новый функционал

---

## 🐛 Специальная осторожность

### 1. Short-circuit evaluation

**Важно:** AND и OR должны использовать short-circuit evaluation.

```python
# Если первый operand = false, второй НЕ вычисляется
1 > 2 and undefined_var < 10  # O ОШИБКА, если вычислить undefined_var

# Решение: вычислять operands одного за другим, рано выходить
for operand in operands:
    result = result and operand
    if not result:
        break  # ← Важно!
```

### 2. Right-associativity для if-else

**Важно:** Если-выражения должны быть right-associative.

```clc
# Это должно паться как: A if B else (C if D else E)
# А не: ((A if B else C) if D else E)
a if b else c if d else e
```

Lark обрабатывает это автоматически через `recursive` правила.

### 3. Цепочки сравнений

**Важно:** `1 < x < 10` должны означать `(1 < x) and (x < 10)`.

```python
# В _eval_comparison():
for i in range(0, len(children) - 2, 2):
    left = self._to_decimal(...)
    op = children[i + 1].value
    right = self._to_decimal(...)
    result = result and (left OP right)
```

### 4. Приоритет `not` ниже сравнения

**Важно:** `not x < 5` означает `not (x < 5)`, а не `(not x) < 5`.

Это происходит автоматически, если `not_expr` вызывает `comparison`, а не `unary`.

```lark
?not_expr: "not" not_expr      # ← Вызывает itself, может быть вложенный
         | comparison           # ← Не вызывает comparison прямо из not!
```

### 5. Булевские значения только в условиях

**Важно:** Проверять на каждом присваивании и в арифметике.

```python
if isinstance(value, bool):
    raise BooleanContextError(
        f"Cannot assign boolean value...",
        line=...,
        column=...
    )
```

---

## ✔️ Чек-лист перед отправкой

### Функционал
- [ ] Парсер может разобрать условные выражения
- [ ] Парсер может разобрать сравнения
- [ ] Парсер может разобрать логические операции
- [ ] Интерпретатор вычисляет условния правильно
- [ ] Интерпретатор отклоняет боолевские присваивания
- [ ] Интерпретатор отклоняет boolean в арифметике
- [ ] Short-circuit evaluation работает

### Тестирование
- [ ] 57 новых тестов проходят
- [ ] 19 старых тестов продолжают работать
- [ ] mypy: 0 ошибок типизации
- [ ] Никаких остальных warnings/errors

### Совместимость
- [ ] Старые примеры программ работают (fib.clc и т.д.)
- [ ] Старые test cases проходят
- [ ] Нет регрессий в функциональности

### Документация
- [ ] README обновлен
- [ ] Примеры добавлены
- [ ] Docstrings добавлены
- [ ] Нет грамматических ошибок

---

## 🎓 Полезные ссылки

| Документ | Для | Читать если |
|----------|-----|---|
| DESIGN_SUMMARY.md | Обзор | Нужен быстрый обзор |
| DESIGN_CONDITIONALS.md | Требования | Нужна полная спецификация |
| IMPLEMENTATION_PLAN.md | Кодинг | Начинаете писать code |
| OPERATOR_PRECEDENCE.md | Тестирование | Тестируете парсер |
| SYNTAX_EXAMPLES.md | Примеры | Нужны примеры синтаксиса |
| TEST_SCENARIOS.md | Тестирование | Пишете тесты |

---

## 🚀 Итого

**Примерное время реализации:**
- Грамматика: 1 час
- Интерпретатор: 2-3 часа
- Обновления и проверки: 1-2 часа
- Тестирование: 1-2 часа
- Документация: 30 минут
- **Итого: 6-10 часов работы**

**Результаты после завершения:**
- ✅ DSL поддерживает условные выражения
- ✅ DSL поддерживает сравнения и логику
- ✅ Система типов гарантирует безопасность
- ✅ Все 76 тестов проходят (57 + 19)
- ✅ Нет регрессий, обратная совместимость сохранена
- ✅ Документация полная

---

**Вы в курсе, что делать? Вперед! 🎯**
