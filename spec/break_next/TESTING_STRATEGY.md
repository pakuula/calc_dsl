# 🧪 Стратегия Тестирования Break и Next

**Версия:** 1.0  
**Дата:** 2026-02-14  
**Статус:** Рекомендуемый процесс  

---

## 📊 Обзор Тестовых Наборов

| Набор | Файл | Кол-во | Назначение |
|-------|------|--------|-----------|
| **Базовые** | `test_break_next.py` | 30 | Основные сценарии из спецификации |
| **Граничные** | `test_break_next.py` | 13 | Граничные и edge cases |
| **Интеграционные** | `test_break_next.py` | 8 | Сложные сценарии |
| **Обратная совместимость** | `test_break_next.py` | 5 | Проверка старых тестов |
| **Продвинутые** | `test_break_next_advanced.py` | 50+ | Глубокое тестирование |
| **ИТОГО** | - | **100+** | - |

---

## 🎯 Фазы Тестирования

### Фаза 1: Базовая Валидрация (день 1)

**Цель:** Убедиться, что фичи работают по базовому сценарию

```bash
# Запустить базовые тесты
python -m pytest tests/test_break_next.py::TestBreak -v
python -m pytest tests/test_break_next.py::TestNext -v
python -m pytest tests/test_break_next.py::TestBreakNext -v
```

**Критерии успеха:**
- ✅ 30 базовых тестов проходят без ошибок
- ✅ Тестовые сценарии из DESIGN_BREAK_NEXT.md выполняются корректно
- ✅ Результаты совпадают с ожидаемыми значениями

### Фаза 2: Тестирование Ошибок (день 1)

**Цель:** Проверить корректную обработку ошибочных ситуаций

```bash
# Тесты на ошибки
python -m pytest tests/test_break_next.py::TestBreakNextErrors -v
```

**Критерии успеха:**
- ✅ Break вне цикла выбрасывает исключение
- ✅ Next вне цикла выбрасывает исключение
- ✅ Break from несуществующего цикла выбрасывает исключение
- ✅ Сообщения об ошибках информативны

### Фаза 3: Граничные Случаи (день 2)

**Цель:** Проверить поведение на границах

```bash
# Граничные случаи
python -m pytest tests/test_break_next.py::TestBreakNextEdgeCases -v
```

**Критерии успеха:**
- ✅ Break с шагом работает корректно
- ✅ Next с отрицательным диапазоном работает
- ✅ Вложенные циклы с break/next работают корректно
- ✅ Пустые циклы обрабатываются правильно

### Фаза 4: Обратная Совместимость (день 2)

**Цель:** Убедиться, что старые тесты не сломались

```bash
# Проверка совместимости
python -m pytest tests/test_interpreter.py -v
python -m pytest tests/test_loops.py -v
python -m pytest tests/test_conditionals.py -v
python -m pytest tests/test_break_next.py::TestBackwardCompatibility -v
```

**Критерии успеха:**
- ✅ Все 66+ старых тестов проходят
- ✅ Никаких регрессий в поведении

### Фаза 5: Продвинутое Тестирование (день 3)

**Цель:** Глубокое тестирование сложных сценариев

```bash
# Продвинутые тесты
python -m pytest tests/test_break_next_advanced.py -v
```

**Критерии успеха:**
- ✅ Производительность на больших диапазонах
- ✅ Сложные комбинации условий
- ✅ Побочные эффекты обрабатываются корректно
- ✅ Рекурсивные функции работают

---

## 📋 Детали Тестовых Наборов

### TestBreak (10 тестов)

| Тест | Описание | Ожидаемый результат |
|------|---------|-------------------|
| `test_break_simple_unconditional` | Break без условия | 99 |
| `test_break_with_condition_true` | Break когда условие истинно | 50 |
| `test_break_with_condition_false` | Break когда условие ложно | 15 |
| `test_break_accumulate_variable` | Break с аккумулятором | 15 |
| `test_break_nested_inner` | Break из внутреннего цикла | 302 |
| `test_break_nested_outer_from` | Break from внешнего цикла | 54 |
| `test_break_without_condition` | Безусловный break | 777 |
| `test_break_multiple_only_first_triggers` | Множество break's | 300 |
| `test_break_first_iteration` | Break на первой итерации | 42 |
| `test_break_with_complex_expression` | Break с выражением | 76 |

### TestNext (10 тестов)

| Тест | Описание | Ожидаемый результат |
|------|---------|-------------------|
| `test_next_simple_unconditional` | Next без условия | 0 |
| `test_next_with_condition_true` | Next когда условие истинно | 25 |
| `test_next_with_condition_false` | Next когда условие ложно | 15 |
| `test_next_on_last_iteration` | Next на последней итерации | 2 |
| `test_next_collect_values` | Накопление значений | (2) |
| `test_next_nested_inner` | Next во вложенном цикле | 303 |
| `test_next_nested_with_variable` | Next с переменной | 301 |
| `test_next_multiple_conditions` | Множество условий next | 13 |
| `test_next_empty_range` | Next в пустом цикле | None |
| `test_next_with_side_effects` | Next с побочными эффектами | 5 |

### TestBreakNext (4 теста)

- Комбинированное тестирование break и next в одном цикле
- Проверка взаимодействия между конструкциями

### TestBreakNextErrors (6 тестов)

- Тестирование всех видов ошибок
- Проверка корректности исключений

### TestBreakNextEdgeCases (13 тестов)

Дополнительные граничные случаи:
- Циклы с шагом
- Отрицательные диапазоны
- Работа с Decimal
- Функции в break/next

### TestBreakNextIntegration (8 тестов)

Сложные сценарии использования:
- Поиск в структурах
- Подсчет простых чисел
- Генерация последовательностей

### TestBreakNextAdvancedScenarios (50+ тестов)

Продвинутые тесты:
- Производительность на больших диапазонах
- Математические операции
- Сложные условия
- Побочные эффекты
- Рекурсия
- Стресс-тесты

---

## 🔍 Ключевые Точки Проверки

### 1. Корректность Результатов

```python
# Проверить, что результат break = выражение в with
code = "for i in 1 .. 10 (break with 99)"
assert eval_code(code) == 99  ✓

# Проверить, что результат цикла без break = последнее выражение
code = "for i in 1 .. 5 (i * 2)"
assert eval_code(code) == 10  ✓
```

### 2. Управление Потоком

```python
# Проверить, что break выходит из цикла
# Проверить, что next пропускает остаток итерации
# Проверить, что break from выходит из нужного цикла
```

### 3. Побочные Эффекты

```python
# Проверить, что изменения переменных сохраняются после break
# Проверить, что изменения переменных сохраняются после next
# Проверить целостность шага между итерациями
```

### 4. Обработка Ошибок

```python
# Break вне цикла → BreakOutsideLoopError
# Next вне цикла → NextOutsideLoopError
# Break from несуществующего → LoopNotFoundError
```

### 5. Вложенные Циклы

```python
# Проверить, что break без from выходит из ближайшего цикла
# Проверить, что break from выходит из нужного цикла
# Проверить, что next переходит на следующую итерацию нужного цикла
```

---

## 📈 Метрики Тестирования

### Целевое Покрытие

| Компонент | Целевое покрытие |
|-----------|-----------------|
| Break конструкция | 95%+ |
| Next конструкция | 95%+ |
| Вложенные циклы | 90%+ |
| Обработка ошибок | 100% |
| Побочные эффекты | 85%+ |
| Производительность | Не регрессирует |

### Успешное Выполнение

- ✅ **80+ тестов** из `test_break_next.py`
- ✅ **50+ тестов** из `test_break_next_advanced.py`
- ✅ **100% успех** на всех фазах
- ✅ **Все 66+ старых тестов** проходят

---

## 🚀 Команды для Быстрого Запуска

### Запустить все тесты break/next

```bash
python -m pytest tests/test_break_next.py tests/test_break_next_advanced.py -v
```

### Запустить только базовые тесты

```bash
python -m pytest tests/test_break_next.py::TestBreak tests/test_break_next.py::TestNext -v
```

### Запустить тесты с покрытием

```bash
python -m pytest tests/test_break_next.py --cov=interpreter --cov-report=html
```

### Запустить тесты на производительность

```bash
python -m pytest tests/test_break_next_advanced.py::TestBreakNextPerformance -v
```

### Проверить обратную совместимость

```bash
python -m pytest tests/ -v --tb=short
```

---

## 🎓 Рекомендуемый Порядок Тестирования

### День 1: Основные Функции

```
1. TestBreak (10 тестов)
2. TestNext (10 тестов)
3. TestBreakNextErrors (6 тестов)
```

**Ожидаемый результат:** ≥26 успешных тестов

### День 2: Интеграция и Граничные Случаи

```
1. TestBreakNext (4 теста)
2. TestBreakNextEdgeCases (13 тестов)
3. TestBackwardCompatibility (5 тестов)
```

**Ожидаемый результат:** ≥22 успешных теста, ≥66 старых тестов проходят

### День 3: Продвинутые Сценарии

```
1. TestBreakNextIntegration (8 тестов)
2. TestBreakNextAdvancedScenarios (50+ тестов)
3. TestBreakNextPerformance (3 теста)
```

**Ожидаемый результат:** ≥50 успешных тестов

---

## ✅ Чеклист Перед Релизом

- [ ] Все 30 базовых тестов проходят
- [ ] Все тесты ошибок выбрасывают корректные исключения
- [ ] Граничные случаи обработаны корректно
- [ ] Все 66+ старых тестов все еще проходят
- [ ] Нет регрессий в производительности
- [ ] Документация обновлена
- [ ] Code review пройден
- [ ] Тесты производительности успешны

---

## 📝 Комментарии для Тестера

### Когда Писать Новые Тесты

1. **При обнаружении ошибки:** Добавить тест, воспроизводящий ошибку, потом исправить
2. **При добавлении функциональности:** Написать тесты ДО кода
3. **При граничных случаях:** Добавить тест для каждого найденного граничного случая

### Как Организовать Тесты

```
test_break_next.py          # Главный файл с базовыми и интеграционными тестами
test_break_next_advanced.py # Продвинутые сценарии и стресс-тесты
```

### Когда Проверять Совместимость

1. После реализации new конструкции
2. После изменения парсера
3. После оптимизации интерпретатора
4. Перед каждым релизом

---

## 🔧 Отладка Неправильных Тестов

### Если тест не пройден

1. **Проверить ожидаемое значение** из спецификации
2. **Вычислить вручную** требуемый результат
3. **Запустить код отдельно** с трассировкой
4. **Проверить парсинг** синтаксиса
5. **Проверить исключения** если тест на ошибку

### Частые Ошибки

| Ошибка | Решение |
|--------|---------|
| `BreakOutsideLoopError` где неожидается | Проверить, что break внутри цикла |
| Неправильный результат | Перепроверить математику |
| Регрессия в старых тестах | Проверить изменения в интерпретаторе |
| Нарушение области видимости | Проверить инициализацию переменных |

---

**Готово для тестирования!**
