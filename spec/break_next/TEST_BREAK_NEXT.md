# 🧪 Тестовые сценарии: break и next

**Версия:** 1.0  
**Дата:** 2026-02-14  
**Статус:** Для тестирования  

**Целевая аудитория:** @tester  

---

## 📋 Обзор

Документ описывает все тестовые сценарии для конструкций `break` и `next`. Включает:
- Базовые тесты (простые случаи)
- Граничные случаи (edge cases)
- Тесты обратной совместимости (66 существующих тестов)
- Тесты ошибок

---

## 🎯 Структура тестов

Все тесты должны быть добавлены в отдельный файл `tests/test_break_next.py` в соответствующие классы:

```python
class TestBreak(unittest.TestCase):
    """Тесты конструкции break"""
    
class TestNext(unittest.TestCase):
    """Тесты конструкции next"""
    
class TestBreakNext(unittest.TestCase):
    """Комбинированные тесты (break + next)"""
    
class TestBreakNextErrors(unittest.TestCase):
    """Тесты на ошибки"""
    
class TestBackwardCompatibility(unittest.TestCase):
    """Обратная совместимость: все 66 тестов должны пройти"""
```

---

## 📝 Break тесты

### TB-001: Простой break (безусловный)

**Программа:**
```clc
for i in 1 .. 10 (
    break with 99
)
```

**Ожидаемый результат:** `99`

**Комментарий:** Цикл завершается на первой итерации, результат = выражение из `with`.

---

### TB-002: Break с условием - условие истинно

**Программа:**
```clc
for i in 1 .. 10 (
    break when i == 5 with i * 10
)
```

**Ожидаемый результат:** `50`

**Комментарий:** Когда i=5, условие истинно, цикл завершается с результатом 50.

---

### TB-003: Break с условием - условие ложно

**Программа:**
```clc
sum = 0
for i in 1 .. 5 (
    sum += i
    break with 999 when i == 100  # условие никогда не будет истинно
)
sum
```

**Ожидаемый результат:** `15` (1+2+3+4+5)

**Комментарий:** Условие никогда не сработает, цикл выполнится полностью, результат = последнее выражение (sum).

---

### TB-004: Break накапливает в переменной

**Программа:**
```clc
sum = 0
for i in 1 .. 10 (
    sum += i
    break when i == 5 with sum
)
```

**Ожидаемый результат:** `15` (1+2+3+4+5)

**Комментарий:** Break возвращает значение переменной sum в момент выхода.

---

### TB-005: Break с вложенными циклами - выход из внутреннего

**Программа:**
```clc
result = 0
for i in 1 .. 3 (
    for j in 1 .. 10 (
        break when j == 3 with j
        result = i * 100 + j
    )
)
result
```

**Ожидаемый результат:** `302` (3*100 + 2 из последней итерации i=3)

**Комментарий:** Break без `from` выходит из ближайшего цикла (по j), но цикл по i продолжает работать. Трассировка:
- i=1: j=1 → result=101; j=2 → result=102; j=3 → break (выход из j)
- i=2: j=1 → result=201; j=2 → result=202; j=3 → break (выход из j)
- i=3: j=1 → result=301; j=2 → result=302; j=3 → break (выход из j) [ПОСЛЕДНЯЯ ИТЕРАЦИЯ i]
- Результат = 302 (последнее выполненное выражение в последней итерации i)

---

### TB-006: Break с вложенными циклами - break from внешнего

**Программа:**
```clc
result = 0
for i in 1 .. 100 (
    for j in 1 .. 100 (
        break from i when i * j > 50 with i * j
        result = i * j
    )
)
result
```

**Ожидаемый результат:** `54` (6 * 9)

**Комментарий:** `break from i` выходит из цикла по i, завершая оба цикла. При i=6, j=9: 6*9=54 > 50, выполняется break from i with 54.

---

### TB-007: Break без условия (безусловный выход)

**Программа:**
```clc
result = 0
for i in 1 .. 10 (
    result = i * 2
    break with 777
)
```

**Ожидаемый результат:** `777`

**Комментарий:** Break без условия (when) выполняется всегда. На i=1: result=2, затем сразу break with 777. Результат цикла = 777.

---

### TB-008: Несколько break в цикле (только первый срабатывает)

**Программа:**
```clc
result = 0
for i in 1 .. 10 (
    break when i == 3 with 300
    break when i == 5 with 500
    result = i
)
result
```

**Ожидаемый результат:** `300`

**Комментарий:** Первый break с условием i==3 срабатывает, второй никогда не достигается (код после break не выполняется).

---

### TB-009: Break без выражения - должна ошибка

**Программа:**
```clc
for i in 1 .. 10 (
    break
)
```

**Ожидаемая ошибка:** Синтаксическая ошибка или `SyntaxError`

**Комментарий:** `with expr` обязательна в break.

---

### TB-010: Break from несуществующего цикла - ошибка

**Программа:**
```clc
for i in 1 .. 10 (
    break from j with i
)
```

**Ожидаемая ошибка:** `LoopNotFoundError: цикл по переменной 'j' не найден`

---

---

## 📝 Next тесты

**Ключевое правило:** Результат цикла равен результату **последней выполненной итерации**. Результат итерации — это последнее значение выражения, вычисленное перед `next`. Если в последней итерации цикла выполнился `next`, результат этой итерации не меняется, она все равно считается последней.

### TN-001: Простой next (безусловный)

**Программа:**
```clc
sum = 0
for i in 1 .. 5 (
    next
    sum += i  # никогда не выполнится
)
sum
```

**Ожидаемый результат:** `0`

**Комментарий:** Next пропускает весь остальной код в каждой итерации, поэтому `sum += i` никогда не выполняется. Результат цикла = результат последней итерации. В последней итерации (i=5) выполнился только next, поэтому результат этой итерации = null. Возвращаемый результат цикла = null, но так как потом мы возвращаем переменную sum, она остается 0.

---

### TN-002: Next с условием - условие истинно

**Программа:**
```clc
sum = 0
for i in 1 .. 10 (
    next when i mod 2 == 0  # пропустить четные
    sum += i
)
sum
```

**Ожидаемый результат:** `25` (1+3+5+7+9)

**Комментарий:** Next сработает для четных, нечетные суммируются.

---

### TN-003: Next с условием - условие ложно

**Программа:**
```clc
sum = 0
for i in 1 .. 5 (
    next when false  # никогда не сработает
    sum += i
)
sum
```

**Ожидаемый результат:** `15` (1+2+3+4+5)

**Комментарий:** Условие всегда ложно, next не выполняется, сумма полная.

---

### TN-004: Next на последней итерации

**Программа:**
```clc
result = 0
for i in 1 .. 3 (
    if i == 3 then
        next
    result = i
)
result
```

**Ожидаемый результат:** `2`

**Комментарий:** Результат цикла равен результату последней выполненной итерации. На i=1,2 выполняется `result = i`. На i=3 (последняя итерация) выполняется next, который пропускает присваивание. Результат последней итерации (i=3) = null (ничего не выполнилось). Результат цикла = результат i=2 = 2. Переменная result содержит 2 из-за побочного эффекта на итерации i=2.

---

### TN-005: Next со списком всех итераций

**Программа:**
```clc
values = ()
for i in 1 .. 5 (
    next when i != 2  # пропустить все кроме i=2
    values += (i)
)
values
```

**Ожидаемый результат:** `(2)` (кортеж с одним элементом)

**Комментарий:** Только для i=2 не выполнится next.

---

### TN-006: Next в вложенном цикле - ближайший

**Программа:**
```clc
result = 0
for i in 1 .. 3 (
    for j in 1 .. 3 (
        if j == 2 then
            next  # пропустить j=2
        result = i * 100 + j
    )
)
result
```

**Ожидаемый результат:** `303` (3*100 + 3)

**Комментарий:** Next без переменной пропускает внутренний цикл (j), внешний цикл (i) продолжает нормально.

---

### TN-007: Next с явной переменной - выход из вложенного

**Программа:**
```clc
result = 0
for i in 1 .. 3 (
    for j in 1 .. 3 (
        if j == 2 then
            next i  # перейти к следующей итерации по i
        result = i * 100 + j
    )
)
result
```

**Ожидаемый результат:** `301` (3*100 + 1 из последней итерации i=3, j=1)

**Комментарий:** `next i` завершает текущий цикл j и переходит к следующей итерации по i. Результат цикла по i = результат последней выполненной итерации = результат i=3 = 301. 
- i=1, j=1: result = 101; j=2: next i
- i=2, j=1: result = 201; j=2: next i  
- i=3, j=1: result = 301 [ПОСЛЕДНЯЯ ИТЕРАЦИЯ i]; j=2: next i (конец)
- Возвращаемое значение result переменной = 301

---

### TN-008: Несколько next в цикле

**Программа:**
```clc
sum = 0
for i in 1 .. 10 (
    next when i mod 2 == 0   # пропустить четные
    next when i mod 3 == 0   # эта строка никогда не будет проверена для четных
    sum += i
)
sum
```

**Ожидаемый результат:** `20` (1+5+7)

**Комментарий:** Первый next срабатывает для четных, остальные числа (нечетные) проходят дальше. Из нечетных i=3 и i=9 пропускаются вторым next.

---

### TN-009: Next в пустом цикле

**Программа:**
```clc
for i in 10 .. 1 (
    next
)
```

**Ожидаемый результат:** `null` или `undefined` (значение пустого цикла)

**Комментарий:** Цикл не имеет итераций, результат = результат пустого цикла.

---

### TN-010: Next без переменной - ошибка в контексте

**Программа:**
```clc
next  # вне цикла
```

**Ожидаемая ошибка:** `NextOutsideLoopError: next используется вне цикла`

---

---

## 📝 Комбинированные тесты (Break + Next)

### TC-001: Break и Next в одном цикле

**Программа:**
```clc
result = 0
for i in 1 .. 20 (
    next when i mod 2 == 0      # пропустить четные
    if i > 10 then
        break with i             # выход для нечетных > 10
    result = i
)
result
```

**Ожидаемый результат:** `9`

**Комментарий:** Цикл выполняется:
- i=1,3,5,7,9: не четные, не > 10 → result = i (result становится 1, 3, 5, 7, 9)
- i=2,4,6,8,10: четные → next (пропуск)
- i=11: не четное, > 10 → break with 11 (выход из цикла с результатом 11)
  - **Важно:** `result = i` не выполняется при i=11, поэтому result остается 9
- Программа возвращает переменную result = 9 (не результат цикла)

---

### TC-002: Трехуровневая вложенность с break/next

**Программа:**
```clc
result = 0
for i in 1 .. 3 (
    for j in 1 .. 3 (
        for k in 1 .. 3 (
            next j when k == 2  # перейти к следующей итерации j (не k!)
            result = i * 10000 + j * 100 + k
        )
    )
)
result
```

**Ожидаемый результат:** `30201` (3*10000 + 2*100 + 1)

**Комментарий:** `next j` выходит из самого внутреннего цикла и закрывает цикл по k.

---

### TC-003: Break в одном цикле, next в другом

**Программа:**
```clc
outer_result = 0
for i in 1 .. 100 (
    inner_result = 0
    for j in 1 .. 100 (
        next when j mod 3 == 0      # пропустить j кратные 3
        break from i when i * j > 20 with i     # выход из внешнего цикла
        inner_result += j
    )
    outer_result = i
)
outer_result
```

**Ожидаемый результат:** `1`

**Комментарий:** Внешний цикл (i) выполняется:
- i=1: Внутренний цикл j проходит нормально (1*j <= 20 для всех j до 100). outer_result = 1
- i=2: Во внутреннем цикле при j=11 условие 2*11=22 > 20 становится истинным. 
  - Выполняется: break from i with i (выход из цикла по i с результатом 2)
  - **Важно:** outer_result = i не выполняется, потому что break выходит из цикла по i
- Программа возвращает outer_result = 1 (последнее значение перед break from i)

---

### TC-004: Break и Next в циклах с разными переменными

**Программа:**
```clc
sum = 0
for i in 1 .. 5 (
    for j in 1 .. 5 (
        next when j == 3
        break from i when i + j > 7 with sum
        sum += i * j
    )
)
sum
```

**Ожидаемый результат:** `105` (точное значение требует трассировки)

---

---

## ⚠️ Тесты на ошибки

### TE-001: Break вне цикла

**Программа:**
```clc
break with 5
```

**Ожидаемая ошибка:** `BreakOutsideLoopError: break используется вне цикла`

---

### TE-002: Next вне цикла

**Программа:**
```clc
next
```

**Ожидаемая ошибка:** `NextOutsideLoopError: next используется вне цикла`

---

### TE-003: Break from несуществующей переменной

**Программа:**
```clc
for i in 1 .. 10 (
    break from nonexistent with 5
)
```

**Ожидаемая ошибка:** `LoopNotFoundError: цикл по переменной 'nonexistent' не найден`

---

### TE-004: Next для несуществующей переменной

**Программа:**
```clc
for i in 1 .. 10 (
    next nonexistent
)
```

**Ожидаемая ошибка:** `LoopNotFoundError: цикл по переменной 'nonexistent' не найден`

---

### TE-005: Вложенные циклы с одинаковой переменной (конфликт)

**Программа:**
```clc
for i in 1 .. 10 (
    for i in 1 .. 5 (
        break with i
    )
)
```

**Ожидаемая ошибка:** `DuplicateLoopVariableError: переменная 'i' уже используется во вложенном цикле`

---

### TE-006: Break без with (должна синтаксическая ошибка)

**Программа:**
```clc
for i in 1 .. 10 (
    break when i == 5
)
```

**Ожидаемая ошибка:** `SyntaxError` или парсер должен требовать `with` после `break`

---

---

## ✅ Тесты обратной совместимости

**Требование:** Все 66 существующих тестов должны пройти без изменений.

### Точки проверки:

1. **Арифметика:** тесты сложения, вычитания, умножения
2. **Функции:** определение и вызов функций
3. **Циклы без break/next:** все существующие тесты циклов
4. **Условные выражения:** все тесты if-then-else (условные были реализованы)
5. **Переменные:** область видимости, переменные цикла
6. **Точность:** режим trace, Decimal точность

**Команда для проверки:**
```bash
python -m pytest tests/test_break_next.py -v
```

**Ожидаемый результат:** >=66 тестов пройдено (новые + старые)

---

---

## 🧪 Как писать тесты (шаблон для tests/test_break_next.py)

```python
import unittest
from decimal import Decimal
from interpreter import Interpreter, BreakOutsideLoopError, NextOutsideLoopError

class TestBreak(unittest.TestCase):
    def setUp(self):
        self.interpreter = Interpreter()
    
    def test_break_simple_unconditional(self):
        """TB-001: Простой break (безусловный)"""
        code = """
        for i in 1 .. 10 (
            break with 99
        )
        """
        result = self.interpreter.execute(code)
        self.assertEqual(result, Decimal(99))
    
    def test_break_with_condition_true(self):
        """TB-002: Break с условием - условие истинно"""
        code = """
        for i in 1 .. 10 (
            break when i == 5 with i * 10
        )
        """
        result = self.interpreter.execute(code)
        self.assertEqual(result, Decimal(50))

class TestBreakNextErrors(unittest.TestCase):
    def setUp(self):
        self.interpreter = Interpreter()
    
    def test_break_outside_loop_error(self):
        """TE-001: Break вне цикла - ошибка"""
        code = "break with 5"
        with self.assertRaises(BreakOutsideLoopError):
            self.interpreter.execute(code)

# Аналогично для TestNext, TestBreakNext, TestBackwardCompatibility
```

---

## 📊 Статистика тестов

| Категория | Количество | Статус |
|-----------|-----------|--------|
| Break тесты | 10 | Новые |
| Next тесты | 10 | Новые |
| Комбинированные | 4 | Новые |
| Тесты на ошибки | 6 | Новые |
| Обратная совместимость | 66 | Должны пройти |
| **ИТОГО** | **96** | - |

---

## 🚀 План тестирования

1. **День 1:** Написать и запустить все новые тесты (30 шт)
2. **День 2:** Проверить обратную совместимость (66 старых тестов)
3. **День 3:** Граничные случаи и интеграционные тесты
4. **День 4:** Performance и stress tests (если нужно)

---

**Готово к тестированию!**
