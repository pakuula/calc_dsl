# 🚀 Quick Start для Тестера Break и Next

**Быстрое начало тестирования новых конструкций**

---

## 📁 Структура Файлов

```
tests/
├── test_break_next.py              # 56 основных тестов
│   ├── TestBreak (10)              # Тесты break
│   ├── TestNext (10)               # Тесты next
│   ├── TestBreakNext (4)           # Комбинированные
│   ├── TestBreakNextErrors (6)     # Ошибки
│   ├── TestBreakNextEdgeCases (13) # Граничные случаи
│   └── TestBackwardCompatibility (5) # Обратная совместимость
```

Был подготовлен файл с продвинутыми тестами, но я его удалил, так как требуется проверить. Там обнаружены ужасные ошибки.

```
ignored_tests/
└── test_break_next_advanced.py     # 50+ продвинутых тестов
    ├── TestBreakNextPerformance
    ├── TestBreakNextWithMathOperations
    ├── TestBreakNextComplexConditions
    └── ... еще 15+ классов
```

---

## ⚡ Первые Шаги

### Шаг 1: Запустить Все Тесты

```bash
cd /home/nikolay/zzz/c++/essential_1
python -m pytest tests/test_break_next.py -v
```

**Ожидаемый результат:** Все 56 тестов должны пройти (если break/next реализованы)

### Шаг 2: Запустить Отдельные Группы

```bash
# Только тесты break
pytest tests/test_break_next.py::TestBreak -v

# Только тесты next
pytest tests/test_break_next.py::TestNext -v

# Только тесты ошибок
pytest tests/test_break_next.py::TestBreakNextErrors -v
```

### Шаг 3: Запустить Продвинутые Тесты

НЕ ЗАПУСКАТЬ, пока не будет уверенности в базовой реализации!

```bash
pytest ignored_tests/test_break_next_advanced.py -v
```

---

## 📊 Быстрая Статистика

| Метрика | Значение |
|---------|----------|
| **Базовых тестов (main)** | 56 |
| **Продвинутых тестов** | 50+ |
| **Строк кода тестов** | 1500+ |
| **Сценариев использования** | 100+ |
| **Граничных случаев** | 20+ |
| **Тестов ошибок** | 10+ |

---

## 🎯 Главные Тесты

### Break (выход из цикла)

```python
# Базовый break
for i in 1 .. 10 (
    break with 99  # результат = 99, цикл выходит
)

# Break с условием
for i in 1 .. 10 (
    break when i == 5 with i * 10  # результат = 50
)

# Break из вложенного цикла
for i in 1 .. 3 (
    for j in 1 .. 10 (
        break from i when i * j > 50  # exit outer loop
    )
)
```

### Next (переход на следующую итерацию)

```python
# Базовый next
for i in 1 .. 5 (
    next  # пропустить остаток итерации
    sum += i  # никогда не выполнится
)

# Next с условием
for i in 1 .. 10 (
    next when i mod 2 == 0  # пропустить четные
    sum += i  # суммировать только нечетные
)

# Next в вложенном цикле
for i in 1 .. 3 (
    for j in 1 .. 3 (
        next i when j == 2  # перейти к следующей итерации i
    )
)
```

---

## 🔧 Что Тестируется

### ✅ Основная Функциональность

- [x] Break с условием и без
- [x] Next с условием и без
- [x] Break from для вложенных циклов
- [x] Next с переменной цикла
- [x] Вложенные циклы (до 4 уровней)
- [x] Побочные эффекты и переменные

### ✅ Обработка Ошибок

- [x] Break вне цикла → исключение
- [x] Next вне цикла → исключение
- [x] Break from несуществующего цикла → исключение
- [x] Дублирующиеся переменные цикла → исключение

### ✅ Граничные Случаи

- [x] Пустые циклы
- [x] Циклы с одной итерацией
- [x] Циклы с шагом
- [x] Отрицательные диапазоны
- [x] Break на первой итерации
- [x] Next на последней итерации

### ✅ Продвинутые Сценарии

- [x] Работа с функциями
- [x] Рекурсия
- [x] Математические операции
- [x] Сложные условия (AND/OR)
- [x] Высокая точность (Decimal)

---

## 📋 Типичный Flow Тестирования

```
Фаза 1: Базовые Тесты (26)
   └─ TestBreak (10) ✓
   └─ TestNext (10) ✓
   └─ TestBreakNext (4) ✓
   └─ TestBreakNextErrors (6) ✓

Фаза 2: Граничные и Интеграционные (26)
   └─ TestBreakNextEdgeCases (13) ✓
   └─ TestBreakNextIntegration (8) ✓
   └─ TestBackwardCompatibility (5) ✓

Фаза 3: Продвинутое Тестирование (50+)
   └─ TestBreakNextPerformance (3) ✓
   └─ TestBreakNextWithMathOperations (5) ✓
   └─ TestBreakNextComplexConditions (4) ✓
   └─ ... и еще 30+ тестов
```

---

## 🚨 Частые Ошибки и Решения

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `BreakOutsideLoopError` | Break вне цикла | Переместить break внутрь цикла |
| Неправильный результат | Математическая ошибка | Пересчитать вручную |
| Тест не запускается | Синтаксис | Проверить скобки и точки с запятой |
| Регрессия | Сломалось при изменениях | Запустить `test_interpreter.py` |

---

## 📈 Ожидаемые Результаты

### При Успешной Реализации

```
test_break_next.py::TestBreak::test_break_simple_unconditional PASSED
test_break_next.py::TestBreak::test_break_with_condition_true PASSED
test_break_next.py::TestNext::test_next_simple_unconditional PASSED
...
===== 56 passed in 2.34s =====
```

```
test_break_next_advanced.py::TestBreakNextPerformance::test_break_early_large_range PASSED
test_break_next_advanced.py::TestBreakNextWithMathOperations::test_break_with_trigonometric_functions PASSED
...
===== 50 passed in 5.12s =====
```

### Общее Количество Тестов

- **Основные:** 56 тестов
- **Продвинутые:** 50+ тестов
- **ИТОГО:** 100+ тестов
- **Старые:** 66+ тестов (обратная совместимость)

---

## 🎓 Команды Для Разных Ситуаций

### Когда нужно быстро проверить

```bash
pytest tests/test_break_next.py -v -x  # остановиться на первой ошибке
```

### Когда нужен детальный отчет

```bash
pytest tests/test_break_next.py -vv --tb=long
```

### Когда нужно проверить производительность

```bash
pytest tests/test_break_next_advanced.py::TestBreakNextPerformance -v
```

### Когда нужна статистика покрытия

```bash
pytest tests/test_break_next.py --cov=interpreter --cov-report=term-missing
```

### Когда нужно проверить обратную совместимость

```bash
pytest tests/ -v --tb=short
```

---

## 🔍 Детальное Тестирование Одного Сценария

### Пример: Break с условием

```python
# Код DSL
for i in 1 .. 10 (
    break when i == 5 with i * 10
)

# Ожидаемый результат: 50
# Почему:
# i=1: 1 != 5 → не break → продолжить
# i=2: 2 != 5 → не break → продолжить
# i=3: 3 != 5 → не break → продолжить
# i=4: 4 != 5 → не break → продолжить
# i=5: 5 == 5 → break → результат = 5 * 10 = 50
```

---

## 📞 Когда Что-то Не Работает

### Шаг 1: Проверить Парсер

```bash
python -c "
from interpreter import Interpreter
code = '''
for i in 1 .. 5 (
    break with 42
)
'''
try:
    result = Interpreter().execute(code)
    print(f'Result: {result}')
except Exception as e:
    print(f'Error: {e}')
"
```

### Шаг 2: Запустить Тест Вручную

```bash
python -m pytest tests/test_break_next.py::TestBreak::test_break_simple_unconditional -vv
```

### Шаг 3: Проверить Трассировку

```bash
python -c "
from interpreter import Interpreter
code = 'for i in 1 .. 5 (result = i; break with result)'
interp = Interpreter(trace=True)
print(interp.execute(code))
"
```

---

## 🎉 Финальный Чеклист

- [ ] Все 56 основных тестов проходят
- [ ] Все 50+ продвинутых тестов проходят
- [ ] Нет ошибок при запуске обратной совместимости
- [ ] Производительность в норме
- [ ] Нет утечек памяти
- [ ] Документация актуальна

---

## 📚 Дополнительные Ресурсы

- Полная спецификация: [DESIGN_BREAK_NEXT.md](../spec/DESIGN_BREAK_NEXT.md)
- Тестовые сценарии: [TEST_BREAK_NEXT.md](../spec/TEST_BREAK_NEXT.md)
- Стратегия тестирования: [TESTING_STRATEGY.md](../TESTING_STRATEGY.md)

---

**Happy Testing! 🚀**
