# 📋 Дизайн: Условные выражения и булевские операторы

**Статус**: ✅ Design Phase Complete (готово к реализации)

**Автор**: @designer

**Дата**: 2026-02-14

**Версия**: v1.0

---

## 📑 Структура документов дизайна

### 1. [DESIGN_CONDITIONALS.md](DESIGN_CONDITIONALS.md) — Общие требования

- Требования по функционалу
- Приоритеты операторов
- Архитектура интерпретатора
- Семантика булевских значений
- Обработка ошибок

### 2. [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md) — Архитектурные детали

- Обновления `grammar.lark`
- Новые методы интерпретатора с полным кодом:
  - `_eval_conditional_expr()`
  - `_eval_or_expr()`
  - `_eval_and_expr()`
  - `_eval_not_expr()`
  - `_eval_comparison()`
- Обновления существующих методов
- Проверочный лист

### 3. [OPERATOR_PRECEDENCE.md](OPERATOR_PRECEDENCE.md) — Конкретные примеры

- Визуальная диаграмма приоритетов (9 уровней)
- Примеры парсинга для каждого оператора
- Таблица правил вывода типов
- Матрица ошибок типизации

### 4. [SYNTAX_EXAMPLES.md](SYNTAX_EXAMPLES.md) — Примеры использования

- 6 категорий примеров:
  1. Основные условные выражения
  2. Операторы сравнения
  3. Логические операторы (and, or, not)
  4. Комбинированные выражения
  5. Ошибки и граничные случаи
  6. Практические примеры (налог, факториал, GCD)
- Таблица тестовых выражений (20+ примеров)

### 5. [TEST_SCENARIOS.md](TEST_SCENARIOS.md) — Полное тестирование

- **57 тестов** в 6 категориях:
  1. Базовые условные выражения (9 тестов)
  2. Операторы сравнения (11 тестов)
  3. Логические операторы (15 тестов)
  4. Ошибки типизации (8 тестов)
  5. Комплексные сценарии (8 тестов)
  6. Граничные случаи (6 тестов)
- Готовые к реализации pytest-стили

---

## 🎯 Ключевые проектные решения

### 1. Приоритеты операторов (от низшего к высшему)

```
9. if-else (условное)
8. or (логическое ИЛИ)
7. and (логическое И)
6. not (логическое НЕ)
5. == != < <= > >= (сравнение)
4. + - (сумма)
3. * / mod (произведение)
2. ** (степень)
1. унарный +/-, функции (высший)
```

**Примеры**:
- `1 + 2 < 3` → `(1 + 2) < 3` → `3 < 3` → false
- `not x < 5` → `not (x < 5)`
- `x and y < 5` → `x and (y < 5)` (x должен быть bool!)

### 2. Типизация

**Две системы типов сосуществуют**:

| Контекст | Допустимый тип | Пример |
|----------|---|---|
| Арифметика | Decimal | `x = 5 + 3` ✅ |
| Присваивание | Decimal | `x = 10` ✅ |
| Условие if | boolean | `if x < 5` ✅ |
| Логические op. | boolean | `x and y` ✅ |
| **Присвоение bool** | **❌** | `x = 1 < 2` ❌ |
| **Boolean в math** | **❌** | `(1<2) + 5` ❌ |

### 3. Грамматика

**Новая иерархия выражений**:

```lark
?expr: conditional_expr

?conditional_expr: or_expr
                 | or_expr "if" or_expr "else" conditional_expr

?or_expr: and_expr ("or" and_expr)*
?and_expr: not_expr ("and" not_expr)*
?not_expr: "not" not_expr | comparison
?comparison: sum (COMP_OP sum)*

COMP_OP: "==" | "!=" | "<" | "<=" | ">" | ">="
```

**Важные свойства**:
- `conditional_expr` RIGHT-ASSOCIATIVE (для вложенных if-else)
- `comparison` поддерживает цепочки: `1 < x < 10`
- Boolean значения **локализованы** в условных выражениях

### 4. Short-circuit evaluation

```python
# AND: если первый operand = false, второй не вычисляется
1 > 2 and undefined_var < 10  # false (undefined_var не проверяется)

# OR: если первый operand = true, второй не вычисляется
1 < 2 or undefined_var < 10  # true (undefined_var не проверяется)
```

### 5. Обработка ошибок

Новое исключение:

```python
class BooleanContextError(DSLError):
    """Булевское значение в недопустимом контексте."""
```

**Ошибки**:
- ❌ `x = (1 < 2)` → BooleanContextError
- ❌ `y = (1 < 2) + 5` → BooleanContextError
- ❌ `z = 10 if 5 else 20` → DSLError (condition не boolean)

---

## 📊 План реализации

### Этап 1: Грамматика (1-2 часа)

- Обновить `grammar.lark` (+ 20 строк)
- Тестировать парсинг

### Этап 2: Интерпретатор — базовая структура (2-3 часа)

- 5 новых методов (180+ строк кода)
- Обновить `_eval()` для новых типов узлов
- Новое исключение `BooleanContextError`

### Этап 3: Проверка типов (1-2 часа)

- Обновить `_eval_assignment()` (10 строк)
- Обновить арифметические методы (10 строк per method)
- Обработка boolean в функциях

### Этап 4: Тестирование (2-3 часа)

- 57 новых тестов (TEST_SCENARIOS.md)
- Проверка обратной совместимости
- mypy validation

**Итого**: ~6-10 часов работы для @coder

---

## ✅ Контрольный список для реализации

### Грамматика
- [ ] Добавить `COMP_OP` токены
- [ ] Добавить правила `conditional_expr`, `or_expr`, `and_expr`, `not_expr`, `comparison`
- [ ] Заменить `?sum` в иерархии
- [ ] Протестировать парсинг

### Интерпретатор — новые методы
- [ ] `_eval_conditional_expr()` (25 строк)
- [ ] `_eval_or_expr()` (20 строк)
- [ ] `_eval_and_expr()` (20 строк)
- [ ] `_eval_not_expr()` (15 строк)
- [ ] `_eval_comparison()` (30 строк)
- [ ] Новое исключение `BooleanContextError`

### Интерпретатор — обновления
- [ ] Обновить `_eval()` для новых типов узлов (10 новых elif блоков)
- [ ] Обновить `_eval_assignment()` для проверки boolean (5 строк)
- [ ] Обновить `_eval_sum()` для проверки boolean в операндах (5 строк)
- [ ] Обновить `_eval_product()` для проверки boolean (5 строк)
- [ ] Обновить `_eval_power()` для проверки boolean (5 строк)

### Тестирование
- [ ] 9 тестов базовых условных выражений
- [ ] 11 тестов операторов сравнения
- [ ] 15 тестов логических операторов
- [ ] 8 тестов ошибок типизации
- [ ] 8 тестов комплексных сценариев
- [ ] 6 тестов граничных случаев
- [ ] Проверка обратной совместимости (старые тесты должны работать)

### Документация
- [ ] Обновить README.md
- [ ] Добавить примеры синтаксиса
- [ ] Обновить docstrings методов
- [ ] Добавить примеры программ

---

## 🔍 Примеры для валидации дизайна

### Пример 1: Простое условие
```clc
result = 10 if 5 < 10 else 20
# result = 10.0000000000
```

### Пример 2: Вложенные условия
```clc
score = 85
grade = 5 if score >= 90 else (4 if score >= 80 else 3)
# grade = 4.0000000000
```

### Пример 3: Логические операции
```clc
x = 10
y = 5
valid = 100 if x > y and y > 0 else -1
# valid = 100.0000000000
```

### Пример 4: Ошибка типизации
```clc
x = (1 < 2)
# ❌ ERROR: Cannot assign boolean value to variable 'x'
```

### Пример 5: Short-circuit evaluation
```clc
# Если 1 > 2 = false, то undefined_var не проверяется
result = 10 if (1 > 2) and (undefined_var < 10) else 20
# result = 20.0000000000
```

---

## 📈 Ожидаемые результаты

### Функционал
- ✅ Условные выражения `A if C else B`
- ✅ Операторы сравнения `==, !=, <, <=, >, >=`
- ✅ Логические операторы `and, or, not`
- ✅ Булевские значения только в условиях
- ✅ Обработка ошибок при неправильном использовании
- ✅ Обратная совместимость со старыми скриптами

### Тестирование
- ✅ 57/57 новых тестов проходят
- ✅ Все 19 старых тестов продолжают работать
- ✅ mypy: 0 ошибок
- ✅ 100% coverage условных выражений

### Документация
- ✅ README обновлен примерами
- ✅ Docstrings для новых методов
- ✅ Примерные программы (conditional.clc, logic.clc)

---

## 🚀 Передача @coder

**Файлы для работы**:
1. [grammar.lark](grammar.lark) — обновить выражения
2. [interpreter.py](interpreter.py) — добавить методы
3. [tests/test_interpreter.py](tests/test_interpreter.py) — добавить тесты

**Справочные документы** (читать перед кодингом):
- DESIGN_CONDITIONALS.md — понять требования
- IMPLEMENTATION_PLAN.md — увидеть структуру кода
- OPERATOR_PRECEDENCE.md — понять приоритеты
- SYNTAX_EXAMPLES.md — примеры использования

**Инструменты**:
- Python 3.13
- Lark парсер (уже установлен)
- pytest для тестирования
- mypy для проверки типов

---

## 📝 История версий

| Версия | Дата | Статус | Описание |
|--------|------|--------|---------|
| v1.0 | 2026-02-14 | ✅ Draft | Завершен дизайн, готово к реализации |

---

**Вопросы для обсуждения**:

1. ✅ Только булевские значения в условных выражениях? (Ограничение)
2. ✅ Short-circuit evaluation? (Требуется для оптимизации)
3. ✅ Цепочки сравнений (`1 < x < 10`)? (Включить)
4. ✅ Приоритет `not` ниже сравнения? (Да: `not x < 5` == `not (x < 5)`)
5. ✅ Right-associativity для вложенных if-else? (Да: `A if B else C if D else E`)

**Все согласованы! 🎉**

---

**Готово к передаче @coder на реализацию.**
